#Labels
    #need to import QLabels from QtWidgets
    #chose QDialog as parent class, can even do QMainWindow,
        #but dialog looks better i guess

#to create layout, used vbox layout from QVBoxLayout
#create label obj of QLabel("pyqt5 label")
#vbox.addWidget(label)
#need to set layout
    #self.setlayout(vbox)

#setting font of label
    #label.setFont("type",size)
#color of label
    #label.setStyleSheet('color:red')